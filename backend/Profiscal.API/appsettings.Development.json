{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning",
      "Microsoft.Hosting.Lifetime": "Information",
      "System.Net.Http.HttpClient": "Warning",
      "System": "Warning"
    }
  },
  "Seed": {
    "AdminEmail": "yosr.charrada@esprit.tn",
    "AdminPassword": "Admin#Taxmind2026",
    "AdminFirstName": "Yosr",
    "AdminLastName": "Charrada"
  },

  "__HOW_TO_SET_SECRETS": "Never commit real secrets here. Set real Neo4j/OpenAI secrets via dotnet user-secrets (see backend/README.md) or environment variables. The Jwt:Key below is a DEV-ONLY placeholder — override it in production.",

  "Jwt": {
    "Key": "dev-only-local-jwt-signing-key-CHANGE-before-any-shared-deployment-taxmind-2026!!"
  },

  "Neo4j": {
    "Uri": "neo4j://127.0.0.1:7687",
    "Username": "neo4j",
    "Password": "",
    "Database": "taxmindvf"
  },
  "OpenAI": {
    "ChatModel": "gpt-4o",
    "Endpoint": "",
    "ApiKey": "",
    "ApiVersion": "2024-02-15-preview"
  },
  "EmbedServer": {
    "Url": "http://127.0.0.1:8081/embed_search"
  },
  "Documents": {
    "Root": ""
  },
  "__ES_AUTOSTART_NOTE": "OFF by default. Set AutoStart:Enabled=true on a machine where you'd rather `dotnet run` bring Elasticsearch up for you instead of opening a terminal for elasticsearch.bat and remembering the --force reindex. It only AUTOMATES those steps — the ES binaries (Path) and the processed_documents corpus must already be on disk; ES cannot be embedded in the build. It never blocks startup: any failure is logged and the app continues (search then behaves as if ES is down). HeapMb caps the ES heap so it doesn't try to grab ~16 GB and crash on a smaller PC. Leave Indexer:* empty to manage the index yourself; set them to auto-run the loader once when the index is empty (needs python + the indexer deps installed).",
  "Elasticsearch": {
    "Host": "http://localhost:9200",
    "Index": "tunisian_legal",
    "AutoStart": {
      "Enabled": false,
      "Path": "C:\\es\\elasticsearch-8.13.0\\bin\\elasticsearch.bat",
      "HeapMb": 1024,
      "ReadyTimeoutSeconds": 180,
      "Indexer": {
        "Command": "",
        "WorkingDirectory": "C:\\taxmind_index_build"
      }
    }
  },

  "__REGISTRATION_NOTE": "Domains an admin may create accounts for. UserAdminService defaults to tn.ey.com alone when this is unset, which would reject the esprit.tn accounts this build seeds.",
  "Registration": {
    "AllowedDomains": "esprit.tn,tn.ey.com,gmail.com"
  },

  "__SMTP_NOTE": "Left EMPTY on purpose: with Smtp:Host blank the service reports IsConfigured=false and the UI shows the generated password instead of mailing it — the safe local default. To actually deliver credential emails, fill Host/User/Password/From with YOUR OWN account (Gmail needs an App Password). OverrideTo is a TESTING switch that redirects EVERY credentials email to one inbox — leave it empty unless you are deliberately testing the mail flow, and never point it at someone else's mailbox.",
  "Smtp": {
    "Host": "",
    "Port": 587,
    "User": "",
    "Password": "",
    "From": "",
    "FromName": "EY Taxmind",
    "EnableSsl": "true",
    "CopyCredentialsTo": "",
    "OverrideTo": ""
  }
}
